/**
 * Base screen layout: SafeAreaView + optional ScrollView, theme padding.
 * Alias for PageContainer (recommended name in Expo/RN structure).
 */

export { PageContainer as Screen } from './PageContainer';
