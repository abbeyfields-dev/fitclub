/**
 * Re-export theme hooks so consumers can import from hooks.
 * Implementation lives in theme/ThemeContext and theme/useAppTheme.
 */

export { useTheme, useThemeContext, useAppTheme } from '../theme';
